/* prueba de allocArray y checkIndex */
/* da error en ejecución: índice 10 fuera de los límites del array */
let
  type  intArray = array of int
  var arr : intArray := intArray [10] of 0
  var i : int := 0
in
  for i := 0 to 10 do (
    arr[i] := i
  );
  arr[4]
end
